name: Generate Pinout
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate Pinout
        uses: splitbrain/pinoutleaf-action@v2
        with:
          config: 'nucleo.yaml' # имя вашего файла с данными
